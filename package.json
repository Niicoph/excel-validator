{
  "name": "excel-validator",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "make:windows": "npm run build && electron-builder --win",
    "make": "npm run build && electron-builder ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run build:renderer && electron .",
    "build": "cd renderer && npm install && npm run build",
    "start": "electron ."


  },
  "author": "Niicoph",
  "license": "MIT",
  "description": "Verifies Excel files for compliance with specific rules and standards.",
  "devDependencies": {
    "electron": "^36.4.0",
    "electron-builder": "^26.0.12"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
